**Comparison Target**

- Source visual truth: `/Users/shiwenjie/github/antchain/web/design-reference.png`
- Browser-rendered implementation: `/Users/shiwenjie/github/antchain/web/qa-implementation-final.png`
- Full-view comparison: `/Users/shiwenjie/github/antchain/web/qa-comparison-final.png`
- Focused trust-rail comparison: `/Users/shiwenjie/github/antchain/web/qa-comparison-trust-final.png`
- Viewport/state: desktop, 1440 × 1024 CSS px, overview dashboard after a successful sandbox evaluation and demo-label issuance.
- Pixel normalization: source 1487 × 1058 px and implementation 1440 × 1024 px, both treated as 1× desktop captures. The full-view comparison normalizes both to 720 × 512 px; the focused comparison normalizes the right trust rails to equal 360 × 720 px panels.

**Findings**

- No actionable P0, P1, or P2 visual findings remain.
- [P3] Lifecycle composition differs from the concept ring.
  Location: central product lifecycle assessment.
  Evidence: the source uses a circular lifecycle path; the implementation uses four lifecycle cards surrounding the same product subject.
  Impact: minor reduction in literal visual fidelity, but the lifecycle hierarchy, product focus, metrics, and order remain clear.
  Fix: optional future refinement if a matching production illustration/ring asset is supplied; the current implementation intentionally avoids fabricated SVG/CSS artwork.
- [P3] Trust rail uses a native digital-label card rather than the source's certification-seal card.
  Location: right trust rail.
  Evidence: both versions contain level/score, QR verification, integrity state, ledger state, and permissions, but the implementation foregrounds live API state and explicitly labels the chain as `PENDING`.
  Impact: acceptable product-specific adaptation that prevents overstating a completed blockchain confirmation.

**Required Fidelity Surfaces**

- Fonts and typography: Chinese system sans fallbacks, weights, hierarchy, compact labels, and score emphasis are coherent and visually aligned with the reference. No broken wrapping or truncation was observed at 1440 × 1024.
- Spacing and layout rhythm: sidebar, header, lifecycle canvas, trust rail, and bottom process strip preserve the source's major-region proportions. Cards, gaps, radii, and borders are consistent; persistent controls remain visible.
- Colors and visual tokens: dark eco-green navigation, light neutral canvas, green success states, and amber pending-chain state map cleanly to the intended trust hierarchy and maintain readable contrast.
- Image quality and asset fidelity: the HES-1000 product is a generated raster asset matched to the source subject and placed without CSS-art or handcrafted-SVG substitutes. The QR is generated by a QR library from the current label ID.
- Copy and content: terminology consistently distinguishes local sandbox calculation, local integrity verification, demo issuance, and an unconfirmed `PENDING` chain state.
- Icons and affordances: one Phosphor icon family is used consistently; navigation, primary CTA, modal, form controls, and verification affordances are implemented as semantic controls.
- Responsiveness/accessibility: desktop target has no clipping or overlap; labels and inputs are associated, buttons are keyboard-reachable, and state is not communicated by color alone. Tablet/mobile refinement remains outside this selected desktop visual target.

**Focused Comparison Evidence**

- The trust-rail comparison confirms that QR presentation, score/level hierarchy, verification facts, chain status, permissions, and label verification remain legible in the implemented rail.
- The implementation intentionally replaces the source's asserted on-chain confirmation with the truthful `演示链状态：PENDING / 本地内存记录，未链上确认` state.

**Interaction and Runtime Evidence**

- Browser-tested sidebar navigation, including a distinct product-management workspace.
- Browser-tested repeated `新建评价 → 创建并签发`; the flow reuses the active algorithm and is idempotent across submissions.
- Browser-tested label verification for `ECO-FFA617A64FB94B2C`; result: `记录存在且本地完整性校验通过；链上仍为 PENDING`.
- Browser console checked after the final flow: no error or warning entries; only Vite debug and React development information messages were present.

**Comparison History**

- Pass 1: generic KPI/header treatment, undersized product image, missing trusted-process strip, and weak trust-rail hierarchy were recorded as P1/P2. The dashboard was rebuilt around the selected product lifecycle visual, the product asset was enlarged, the four-stage trusted process was added, and the trust rail was expanded.
- Pass 2: navigation panels were still duplicated, issuance status contradicted a completed demo issue, and verification wording overstated chain trust. Terra added distinct compact workspaces, changed issuance to `演示环境已签发`, retained the ledger as `PENDING`, and made verification wording state local integrity separately from chain confirmation.
- Final functional pass: a second evaluation initially failed because only one same-name algorithm may be active. Terra changed the frontend to query and reuse the existing active algorithm, then the repeated browser flow completed successfully. The final screenshot and focused trust-rail comparison show no remaining actionable P0/P1/P2 differences.

**Implementation Checklist**

- [x] Match selected option 3's desktop information architecture and eco-green visual system.
- [x] Use a real product raster asset and library icons.
- [x] Connect the primary evaluation, issue, QR, and verification journey to the local API.
- [x] Preserve the sandbox/blockchain boundary and show `PENDING` honestly.
- [x] Verify repeated operation, navigation, production build, tests, and browser console.

**Follow-up Polish**

- If the project receives an approved lifecycle-ring illustration and certification-seal asset, they can replace the current grid treatment without changing the data or trust architecture.

**Workspace Differentiation Iteration — 2026-07-25**

- Browser-rendered evidence: `/Users/shiwenjie/github/antchain/web/qa-products-final.jpg`, `/Users/shiwenjie/github/antchain/web/qa-data-final.jpg`, `/Users/shiwenjie/github/antchain/web/qa-labels-final.jpg`, `/Users/shiwenjie/github/antchain/web/qa-assessments-final.jpg`, `/Users/shiwenjie/github/antchain/web/qa-chain-final.jpg`, `/Users/shiwenjie/github/antchain/web/qa-permissions-final.jpg`, and `/Users/shiwenjie/github/antchain/web/qa-settings-final.jpg`.
- Full-view cross-page comparison: `/Users/shiwenjie/github/antchain/web/qa-workspaces-contact-sheet.jpg`.
- Viewport and density: each workspace was captured at 1440 × 1024 CSS px and 1× density; the contact sheet scales captures uniformly into 500 × 380 px tiles for cross-page information-architecture comparison.
- Source relationship: the selected option-3 source remains the visual-system truth for the persistent sidebar, header, eco-green palette, typography, density, borders, and semantic state colors. The source does not specify these secondary workspaces, so their information architecture is evaluated against the user's requirement for clearly different business workflows rather than invented pixel-level fidelity.
- Full-view evidence: the contact sheet visibly distinguishes a product ledger/detail split, data-package inspector, label list/QR preview, assessment queue and filters, contract evidence browser, permission matrix/view preview, and environment/governance settings.
- Focused evidence: the individual 1440 × 1024 captures were inspected for table density, selected states, QR legibility, contract `PENDING` wording, role-column highlighting, setting switches, clipping, and persistent-action visibility.
- Browser-tested interactions:
  - Product search/status filtering, selected-product detail, and new-product feedback.
  - Data-package selection, local completeness validation, and feedback.
  - API-backed label loading plus copy feedback.
  - Assessment task filtering and queue/empty states.
  - Contract semantic selection with explicit “等待真实链回执 / 不将 PENDING 表述为上链成功”.
  - Partner-role preview and policy-save feedback.
  - AntChain adapter toggle and local settings-save feedback.
- Console iteration: the first label-page render exposed a React duplicate-key warning because API records use `labelId`. Terra normalized `labelId` to the stable UI `id` used by keys, selection, QR, copy, and revoke. After reload and a fresh label-page render, no new console errors or warnings were recorded.
- Result: no actionable P0/P1/P2 layout, fidelity, interaction, or accessibility findings remain for the selected 1440 × 1024 desktop target.

final result: passed
